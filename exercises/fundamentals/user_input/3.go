package main

import "fmt"

func AverageUntilZero() float64 {
	// TODO: read integers in a loop with fmt.Scan until the user enters 0.
	// Return the average (as float64) of the numbers entered before the 0.
	return 0
}

func main() {
	fmt.Println(AverageUntilZero()) // reads "10 20 30 0", prints 20
}