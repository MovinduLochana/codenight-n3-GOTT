package main

import "fmt"

func Report(name string, orders int, revenue float64) string {
	// TODO: use fmt.Sprintf with \n escapes to build the multi-line report
	return ""
}

func main() {
	fmt.Println(Report("Nimal", 12, 3450.5))
}
