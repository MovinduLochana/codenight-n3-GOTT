package main

import "fmt"

func main() {
	// TODO: declare:
	//   name    := "Gopher"   (string)
	//   visits  := 7          (int)
	//   rating  := 4.75       (float64)
	//   renewed := visits > 3 (bool, using a comparison)

	// TODO: print "Welcome back " + name, visits, renewed, rating
	fmt.Println("Welcome back "+name, visits, renewed, rating)
}
