package main

import "fmt"

func main() {
	// TODO: declare count, ratio, part, and open using :=
	//   count  := 5       (int)
	//   ratio  := 7.0     (float64)
	//   part   := "Go"    (string)
	//   open   := 3       (int, later compared)

	// TODO: print count*6, ratio/2.0, part+"lang", and (3 > 2)
	fmt.Println(count*6, ratio/2.0, part+"lang", 3 > 2)
}
