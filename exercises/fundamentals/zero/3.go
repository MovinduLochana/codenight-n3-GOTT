package main

import "fmt"

func main() {
	// TODO: declare `count int`, `price float64`, `name string`,
	// and `done bool` with no assigned value

	// leave `price` and `done` at their zero values
	// TODO: assign count = 9 and name = "Go"

	fmt.Println(count, price, name, done)
}
