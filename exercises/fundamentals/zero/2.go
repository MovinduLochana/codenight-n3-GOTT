package main

import "fmt"

func main() {
	// TODO: declare `count int`, `message string`, and `passed bool`
	// with no assigned value

	// TODO: print count, then "[" + message + "]", then passed
	fmt.Println(count, "["+message+"]", passed)
}
