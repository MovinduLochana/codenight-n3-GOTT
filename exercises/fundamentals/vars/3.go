package main

import "fmt"

// TODO: declare a package-level variable `language` of type string
// (type inferred) with the value "Golang"

func main() {
	// TODO: declare a var block with `year int = 2009` and `isPublic bool = true`

	fmt.Println(language, year, isPublic)
}
