> Implement a function `Swap(a, b *int)` that swaps the values of two integer variables using pointers.
> - In `main()`, declare `x := 10` and `y := 20`.
> - Call `Swap(&x, &y)` and print the values of `x` and `y`.
>
> **Expected Output:**
>
> ```
> x: 20, y: 10
> ```
