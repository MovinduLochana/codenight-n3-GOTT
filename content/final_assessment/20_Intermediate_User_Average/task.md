> Write a program that reads integers one at a time with `fmt.Scan` until the user enters `0`, then prints the average of the numbers entered **before** the `0`.
>
> **Input:** `10 20 30 0`
>
> **Expected Output:**
>
> ```
> Average: 20
> ```