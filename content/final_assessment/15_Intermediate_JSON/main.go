package main

import (
	"encoding/json"
	"fmt"
)

// TODO: Define Product struct with fields: ID, Name, Price, InStock.
// Configure structural JSON tags: id, name, price, in_stock.

// TODO: Implement SerializeProduct(p Product) (string, error).

func main() {
	// Sample product: ID 101, Name "Wireless Mouse", Price 29.99, InStock true
	// TODO: Create the product, serialize it, and print the resulting JSON string.
}
