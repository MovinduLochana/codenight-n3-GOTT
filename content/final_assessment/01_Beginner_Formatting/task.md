> Calculate the average score of a student as a float. You are given two integer variables: `totalScore` (235) and `subjectCount` (3). Perform explicit type conversion to calculate their average, and use `fmt.Printf` or `fmt.Sprintf` to format and print the average to exactly **2 decimal places**.
>
> **Expected Output:**
>
> ```
> Average Score: 78.33
> ```
