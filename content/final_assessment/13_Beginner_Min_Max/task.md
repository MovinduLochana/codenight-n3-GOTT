> Implement a function `FindMinMax(nums []int) (int, int, error)` that finds the minimum and maximum integers in a slice.
> - If the slice is empty or `nil`, return `0`, `0`, and an error with the message `"empty slice"`.
> - Otherwise, return the minimum value, maximum value, and `nil`.
> - In `main()`, print the min and max for `[]int{12, 45, 2, 8, 99, 14}`.
>
> **Expected Output:**
>
> ```
> Min: 2, Max: 99
> ```
