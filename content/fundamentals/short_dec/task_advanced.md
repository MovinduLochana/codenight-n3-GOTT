> Declare `x := 1` and `y := 2`, then swap their values so that `x` becomes `2` and `y` becomes `1`. Use the multiple-assignment form `x, y = y, x` — no temporary variable needed. Print `x` and `y` in that order, space-separated.
>
> **Expected Output:**
>
> ```
> 2 1
> ```