> Declare a package-level variable `language` with the value `"Golang"` (using type inference, *outside* `main`). Inside `main`, declare a `var` block containing `year int = 2009` and `isPublic bool = true`. Print all three in this order: `language`, `year`, `isPublic`.
>
> **Expected Output:**
>
> ```
> Golang 2009 true
> ```