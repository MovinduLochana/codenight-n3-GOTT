> Using a `const` block, define `Radius = 7.0`. Then compute the circumference of a circle with `2 * Pi * Radius`, where `Pi` is also a constant (use `3.14159`). Declare a local variable `circumference := 2 * Pi * Radius` and print it.
>
> **Expected Output:**
>
> ```
> 43.98226
> ```