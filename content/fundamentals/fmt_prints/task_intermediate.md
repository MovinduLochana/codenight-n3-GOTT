> Given the variable `name := "Gopher"`, use `fmt.Printf` to print its value, its quoted form, and its type in this exact format. Use the `%s`, `%q`, and `%T` verbs:
>
> **Expected Output:**
>
> ```
> Name: Gopher, Quoted: "Gopher", Type: string
> ```