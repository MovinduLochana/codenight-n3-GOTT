> Declare four variables with no assigned value: `count int`, `price float64`, `name string`, and `done bool`. Leave `price` and `done` at their zero values, but assign `count = 9` and `name = "Go"`. Print all four in this order: `count`, `price`, `name`, `done`.
>
> **Expected Output:**
>
> ```
> 9 0 Go false
> ```