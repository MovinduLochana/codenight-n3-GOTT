> Declare three variables without assigning values: `count` of type `int`, `message` of type `string`, and `passed` of type `bool`.
>
> Print `count`, then the message wrapped in literal brackets so you can visually confirm the empty-string zero value (`"[" + message + "]"`), then `passed` — all with a single `fmt.Println`.
>
> **Expected Output:**
>
> ```
> 0 [] false
> ```