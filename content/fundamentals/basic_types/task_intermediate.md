> Declare one variable of each of the four basic types with `:=`, then use each value in an expression:
> - `count := 5`, multiply it by `6` → `int`
> - `ratio := 7.0`, divide it by `2.0` → `float64`
> - `part := "Go"`, concatenate it with `"lang"` → `string`
> - `open := 3`, compare `open > 2` → `bool`
>
> Print the four resulting values `count*6`, `ratio/2.0`, `part+"lang"`, and `3 > 2` on one line with `fmt.Println`.
>
> **Expected Output:**
>
> ```
> 30 3.5 Golang true
> ```